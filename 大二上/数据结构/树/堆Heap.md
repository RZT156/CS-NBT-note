# 最大堆
根节点最大
每个结点上的元素值==不小于==其子结点元素的值
# 最小堆
根节点最小
每个结点上的元素值==不大于==其子结点元素的值 
- 从上往下非递减（non-decreasing）
- 先序遍历不一定非递减：左右子树可以左大右小
# 调整（上滤）
1. 先插在最后
2. 不符合一层层向上**比较交换**直到==找到位置==
3. 再检查
# 删除（下滤）
1. 最后一个代替根，根被删除
2. 向下调整