the_minimum_spanning_tree
- **目标**：连接**所有顶点**，并且使用的**边总权重最小**。它关注的是整个网络的**连通成本最低**。
- **性质**：**不保证**任意两顶点之间的路径是原图中的最短路径
判断题：Let M be ==the minimum spanning tree== of a weighted graph G. Then the path in M between V1 and V2 must be ==the shortest path== between them in G.（❌）
# Prim算法
随机挑选一点作顶点

# Kruskal算法
