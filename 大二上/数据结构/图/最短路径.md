the shortest path
- **目标**：从一个指定的**源点**出发，到图中**所有其他顶点**的路径都是**最短路径**。它关注的是从一个点出发到其他所有点的**最短距离**。
- **性质**：在这样一棵以 `V1` 为源点构建的最短路径树中，从 `V1` 到任意顶点 `V2` 的路径，**一定**是原图中从 `V1` 到 `V2` 的==最短路径==。
# Dijkstral算法


# Floyd算法
- 路径矩阵**D**（到自己的距离，即矩阵对角线为0，无关系为∞）
- 前置点矩阵**P**(初始状态全为-1)